language: java
jdk:
- oraclejdk8
branches:
  only:
  - master
cache:
  directories:
  - $HOME/.m2
addons:
  selenium: "2.53.1"
  firefox: "33.0"
  hosts:
    - ciscobinary.openh264.org
script:
  - xvfb-run --server-args="-ac" mvn test -DindexToMine=1 -Dtimeout=55